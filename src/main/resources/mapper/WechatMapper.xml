<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onoff.wechatofficialaccount.mapper.WechatMapper">
    <insert id="addUser" parameterType="com.onoff.wechatofficialaccount.entity.User">
        INSERT INTO wechatuser(openid,nickname,sex,headimgurl,subscribe_time,subscribe_scene,qr_scene,
        qr_scene_str)VALUES(#{openId},#{nickName},#{sex},#{headImgUrl},
        #{subScribeTime},#{subscribeScene},#{qrScene},#{qrSceneStr})
    </insert>
</mapper>